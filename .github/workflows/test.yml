name: Node tests

on:
  pull_request:

concurrency:
  group: node-tests-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR #29 is superseded by PR #31; no release validation runs here."
