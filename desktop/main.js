'use strict';
const {app,BrowserWindow,dialog}=require('electron');const{spawn}=require('child_process');const path=require('path');const http=require('http');let backend=null,win=null;const PORT=8787;function backendRoot(){return app.isPackaged?path.join(process.resourcesPath,'backend'):path.resolve(__dirname,'..');}function startBackend(){const root=backendRoot(),entry=path.join(root,'src','index-v6.js');backend=spawn(process.execPath,[entry,'--web-port',String(PORT),'--web-host','127.0.0.1'],{cwd:root,env:{...process.env,ELECTRON_RUN_AS_NODE:'1'},stdio:['ignore','pipe','pipe']});backend.stdout?.on('data',b=>console.log(String(b).trim()));backend.stderr?.on('data',b=>console.error(String(b).trim()));backend.on('exit',code=>{if(code&&win)dialog.showErrorBox('Analyzer backend stopped',`Backend exited with code ${code}`);});}function waitReady(retries=80){return new Promise((resolve,reject)=>{const ping=()=>{const req=http.get(`http://127.0.0.1:${PORT}/api/status`,res=>{res.resume();resolve();});req.on('error',()=>{if(--retries<=0)reject(new Error('Backend did not start.'));else setTimeout(ping,250);});req.setTimeout(500,()=>req.destroy());};ping();});}async function create(){startBackend();try{await waitReady();}catch(e){dialog.showErrorBox('Startup error',e.message);app.quit();return;}win=new BrowserWindow({width:1500,height:950,minWidth:1100,minHeight:700,backgroundColor:'#0b1017',webPreferences:{nodeIntegration:false,contextIsolation:true,sandbox:true}});await win.loadURL(`http://127.0.0.1:${PORT}`);}app.whenReady().then(create);app.on('window-all-closed',()=>app.quit());app.on('before-quit',()=>{if(backend&&!backend.killed)backend.kill();});
